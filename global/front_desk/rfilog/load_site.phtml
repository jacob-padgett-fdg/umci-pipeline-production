<?
$company_id=addslashes($company_id);
contactsbox("select * from contacts where (contact_type = 'site' or contact_type = 'company') and (company_id = '$company_id' or contacts_id = '$company_id')",$company_id,'site_id','',32);
?>
