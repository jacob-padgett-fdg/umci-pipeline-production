<?
if ($rfi_id=="") die("application error");
$rfi_info=getoneb("select * from rfilog where rfi_id = '$rfi_id'");


$fd_manager=fd_manager($rfi_info->jobinfo_id,$global_user_id);
if ($fd_manager||$adminuser) {
	@mysql_query("delete from rfilog where rfi_id = '$rfi_info->rfi_id'");
	forward('main');
	exit;
	}
die_security();
?>
